<div class="header">
	<div class="header_title">
		<h4>Articles</h4>
	</div>
</div>
<div class="action_button">
	<button class="btn btn-new btn-primary"><%= link_to 'new article', new_article_path %></button>
	<button class="btn btn-new btn-primary"><%= link_to 'new category', new_category_path %></button>	
</div>

<div class="row article">
	<% @articles.each do |article| %>
		<div class="col-md-3">
			<div class="article__wrapper">
				<div class="article__image">
					<%= link_to image_tag("article_1.jpg", :alt => "article", :class => "article__image" ), article_path(article) %>
				</div>

				<%= link_to article_path(article), :class => "article__link" do%>
					<div class="article_content__wrapper">
						<div class="article__title">
							<strong><%= article.title.capitalize %></strong>
						</div>
						<div class="article__content">
							<%= article.text.truncate(150, separator: " ") %>
						</div>				
					</div>
				<% end %>


					
			</div>
		</div>
	<% end %>
</div>
<!-- <table class="blog-list">
	<tr>
		<th>Title</th>
		<th>Content</th>
		<th>Action</th>
	</tr>
	<% @articles.each do |article| %>
		<tr>
			<td class="s-right"><strong><%= article.title %><strong></td>
			<td class="s-both"><%= article.text.truncate(150, separator: ' ') %></td>
			<td class="s-table">
				<button class="btn btn-primary btn-action"><%= link_to 'show', article_path(article) %></button>
			</td>
			<td class="s-table">
				<button class="btn btn-warning btn-action">
					<%= link_to 'edit', edit_article_path(article) %>
				</button>
			</td>
			<td class="s-table">
				<button class="btn btn-danger btn-action">
					<%= link_to 'delete', article_path(article),
						method: :delete,
						data: { confirm: 'Are you sure to delete?' } 
					%>
				</button>
			</td>
		</tr>
	<% end %>
</table> -->



